A simple directory for files built in python.
Functions and dictionary are used for managing the directory
A simple menu with options is diplayed
Upon selection of an option, the relevant function is called that performs the required task
